{
  "name": "nexus-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Nexus Agents - Role Container Management System",
  "scripts": {
    "dev": "pnpm --filter @nexus/frontend dev",
    "dev:frontend": "pnpm --filter @nexus/frontend dev",
    "build": "pnpm -r build",
    "build:frontend": "pnpm --filter @nexus/frontend build",
    "build:backend": "cd platform/backend && go build -o nexus-backend cmd/server/main.go",
    "dev:backend": "cd platform/backend && go run cmd/server/main.go"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
